import { beforeEach, describe, it } from 'vitest';

describe('@hexatool/fs-remove', () => {

	beforeEach(() => {
	});

	describe('sync', () => {
		it('should delete an empty directory', async () => {

		});
	});

	describe('async', () => {
		it('should delete an empty directory', async () => {

		});
	});
});
